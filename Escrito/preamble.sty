% Preámbulo
\ProvidesPackage{preamble}

% Utilizar Símbolos
\usepackage[utf8]{inputenc}

% Elegir Idioma
\usepackage[spanish]{babel}
\usepackage{csquotes}

% Diseño de Página
\usepackage[a4paper]{geometry}
\setlength{\headheight}{13.5pt}

% Agregar Imágenes
\usepackage{graphicx}
\graphicspath{ {images/} }

% Entorno de Ecuaciones
\usepackage{amsmath}
\usepackage{amsthm}
\usepackage{mathtools}
% Especificación de Reglas/Pruebas
\usepackage{bussproofs}
% Ambiente para Reglas/Pruebas
\theoremstyle{definition}
\newtheorem{Prueba}{Prueba}
\newtheorem{Regla}{Regla}
\newtheorem{TRegla}[Regla]{Regla para Tipos}
\newtheorem{DTRegla}[Regla]{Regla para Declaración de Tipos}
\newtheorem{FPRegla}[Regla]{Regla para Función/Procedimiento}
\newtheorem{SRegla}[Regla]{Regla para Sentencias}
\newtheorem{ERegla}[Regla]{Regla para Expresiones}
\newtheorem{ETRegla}[Regla]{Regla para Equivalencia de Tipos}
\newtheorem{PRegla}[Regla]{Regla para Programas}
\newtheorem{Predicado}{Predicado sobre Lectura/Escritura}

% Tabla de Contenidos
\setcounter{tocdepth}{1}

% Estilo de Referencias
\usepackage[sorting = none]{biblatex}
\addbibresource{references.bib}

% Ordenar URLs
\usepackage{xurl}

% Formato de Cabeza / Pie
\usepackage{fancyhdr}
\pagestyle{fancy}
% Cabeza de Página
\fancyhead{}
\fancyhead[RE,LO]{\leftmark}
% Pie de Página
\fancyfoot{}
\fancyfoot[LE,RO]{\thepage}
\fancyfoot[RE,LO]{\rightmark}

% Definición de Colores
\usepackage[dvipsnames]{xcolor}

% Referencias para Navegación
\usepackage{hyperref}
\hypersetup
{
  pdftitle    = {Tesis},
  pdfauthor   = {Matías Federico Gobbi},
  pdfpagemode = UseOutlines,
  colorlinks  = true,
  linkcolor   = OrangeRed,
  citecolor   = PineGreen,
  urlcolor    = Mulberry,
}
\usepackage[all]{hypcap}

% Uso de Comandos
\usepackage{packages/commands}
% Uso de Sintaxis
\usepackage{packages/lang}