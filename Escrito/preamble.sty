% Preámbulo
\ProvidesPackage{preamble}

% Utilizar Símbolos
\usepackage[utf8]{inputenc}

% Elegir Idioma
\usepackage[spanish]{babel}
\usepackage{csquotes}

% Diseño de Página
\usepackage[a4paper]{geometry}
\setlength{\headheight}{13.5pt}

% Agregar Imágenes
\usepackage{graphicx}
\graphicspath{ {images/} }

% Entorno de Ecuaciones
\usepackage{amsmath}

% Estilo de Referencias
\usepackage[style = authoryear]{biblatex}
\addbibresource{references.bib}

% Formato de Cabeza / Pie
\usepackage{fancyhdr}
\pagestyle{fancy}

% Uso de Colores
\usepackage{packages/colors}
% Uso de Comandos
\usepackage{packages/commands}
% Uso de Sintaxis
\usepackage{packages/lang}